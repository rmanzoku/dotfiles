#!/bin/bash
set -eu

usage() {
  cat <<'EOF'
Usage: chezmoi-drift [--apply | --restore]

リポジトリの dot_* ファイルと実ファイルの差分を検出・対応します。

オプション:
  (なし)      差分のあるファイルを一覧表示
  --apply     実ファイルの変更をリポジトリに取り込む (chezmoi re-add)
  --restore   リポジトリの状態に実ファイルを戻す (chezmoi apply)
  --help      このヘルプを表示
EOF
}

info() { printf '\033[1;34m[info]\033[0m %s\n' "$1"; }
warn() { printf '\033[1;33m[warn]\033[0m %s\n' "$1"; }

# chezmoi ignore されている dot_* ファイルの直接比較対象
# dot_ プレフィックスを . に変換したパスと対応
EXTRA_FILES=(
  "dot_claude/CLAUDE.md:.claude/CLAUDE.md"
  "dot_claude/settings.json:.claude/settings.json"
  "dot_claude/skills/skill-manager/SKILL.md:.claude/skills/skill-manager/SKILL.md"
)

check_extra_drift() {
  local repo_root="$1"
  local has_drift=false
  for entry in "${EXTRA_FILES[@]}"; do
    local repo_file="${repo_root}/${entry%%:*}"
    local actual_file="${HOME}/${entry##*:}"
    if [ -f "$repo_file" ] && [ -f "$actual_file" ]; then
      if ! diff -q "$repo_file" "$actual_file" >/dev/null 2>&1; then
        echo "/${entry##*:}"
        has_drift=true
      fi
    elif [ -f "$repo_file" ] && [ ! -f "$actual_file" ]; then
      echo "/${entry##*:} (missing)"
      has_drift=true
    fi
  done
  $has_drift
}

check_drift() {
  local repo_root diff_output has_drift=false
  repo_root="$(git rev-parse --show-toplevel)"

  # chezmoi 管理対象
  diff_output="$(chezmoi diff -S "$repo_root" 2>/dev/null || true)"
  if [ -n "$diff_output" ]; then
    echo "$diff_output" | grep -E '^(---|\+\+\+) [ab]/' | sed 's|^... [ab]/|/|' | sort -u
    has_drift=true
  fi

  # chezmoi ignore 対象（直接比較）
  if check_extra_drift "$repo_root"; then
    has_drift=true
  fi

  if ! $has_drift; then
    info "差分はありません。すべてのファイルがリポジトリと一致しています。"
    return 1
  fi
  return 0
}

confirm() {
  printf '\033[1;33m%s [y/N]: \033[0m' "$1"
  read -r answer
  case "$answer" in
    [yY]|[yY][eE][sS]) return 0 ;;
    *) return 1 ;;
  esac
}

case "${1:-}" in
  --help|-h)
    usage
    ;;
  "")
    info "リポジトリと実ファイルの差分を確認しています..."
    if check_drift; then
      echo ""
      info "上記のファイルに差分があります。"
      info "変更を取り込むには: chezmoi-drift --apply"
      info "管理状態に戻すには: chezmoi-drift --restore"
    fi
    ;;
  --apply)
    info "実ファイルの変更をリポジトリに取り込みます..."
    if ! check_drift; then
      exit 0
    fi
    echo ""
    if confirm "上記のファイルの変更をリポジトリに取り込みますか？"; then
      repo_root="$(git rev-parse --show-toplevel)"
      chezmoi re-add -S "$repo_root"
      # chezmoi ignore 対象は直接コピー
      for entry in "${EXTRA_FILES[@]}"; do
        repo_file="${repo_root}/${entry%%:*}"
        actual_file="${HOME}/${entry##*:}"
        if [ -f "$actual_file" ] && [ -f "$repo_file" ]; then
          if ! diff -q "$repo_file" "$actual_file" >/dev/null 2>&1; then
            cp "$actual_file" "$repo_file"
          fi
        fi
      done
      info "変更を取り込みました。"
    else
      info "キャンセルしました。"
    fi
    ;;
  --restore)
    info "リポジトリの状態に復元します..."
    if ! check_drift; then
      exit 0
    fi
    echo ""
    if confirm "上記のファイルをリポジトリの状態に戻しますか？"; then
      repo_root="$(git rev-parse --show-toplevel)"
      chezmoi apply --force -S "$repo_root"
      # chezmoi ignore 対象は直接コピー
      for entry in "${EXTRA_FILES[@]}"; do
        repo_file="${repo_root}/${entry%%:*}"
        actual_file="${HOME}/${entry##*:}"
        if [ -f "$repo_file" ]; then
          if ! diff -q "$repo_file" "$actual_file" >/dev/null 2>&1; then
            cp "$repo_file" "$actual_file"
          fi
        fi
      done
      info "復元しました。"
    else
      info "キャンセルしました。"
    fi
    ;;
  *)
    echo "不明なオプション: $1" >&2
    usage >&2
    exit 1
    ;;
esac
