# Claude 向けリポジトリ運用ルール

- このリポジトリの成果物は `dot_*` 配下のファイル群であり、chezmoi によって PC 全体の設定として配置される
- Claude Code も `dot_*` 配下のファイルを編集してよい
- `dot_*` 配下を編集するときは、target の実ファイル（例: `~/.local/bin/...`）ではなく、必ずリポジトリ内の source（`dot_*` 配下）を編集し、反映は chezmoi apply のワークフローに従うこと
- `dot_*` 配下の編集では `dotfile-update` スキルのワークフローに従うこと

# 補足

- `dot_*` 以外のリポジトリ運用ファイルの扱いは `AGENTS.md` を参照すること
