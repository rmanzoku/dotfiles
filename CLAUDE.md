# Claude への警告

- このリポジトリの成果物は `dot_*` 配下のファイル群であり、chezmoi によって PC 全体の設定として配置される
- `dot_*` 配下のファイルは Codex の管理対象とする
- Claude Code は `dot_*` 配下のファイルを編集してはいけない
- Claude Code が `dot_*` 配下のファイルの編集を求められた場合は、編集せず「Codex の管理対象であるため自分は変更しない」と警告すること
- `dot_*` 配下を変更する必要がある場合は、Codex に作業を委ねること

# 補足

- `dot_*` 以外のリポジトリ運用ファイルの扱いは `AGENTS.md` を参照すること
