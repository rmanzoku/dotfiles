interface:
  display_name: "Grok CLI Runner"
  short_description: "Run Grok Build headlessly"
  default_prompt: "Use $grok-cli-runner to run a Grok Build CLI headless handoff through a file-based request artifact, including public X/Twitter retrieval when requested, preserving response, summary, stderr, timeout, dry-run, and failure artifacts under .context/<task>/."
